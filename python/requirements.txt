# Core - No external dependencies required!
# The wrapper uses only Python standard library + subprocess to call the DSL compiler

# Optional: for AI integration
# openai>=1.0.0
# anthropic>=0.7.0

# Optional: for development
# pytest>=7.0.0
# black>=23.0.0
# mypy>=1.0.0
